image: node:latest

stages:
  - deploy

production:
  stage: deploy
  before_script:
    - chmod +x ./setup_env.sh
    - ./setup_env.sh
    - npm install
    - npm install -g serverless
  script:
    - serverless deploy --verbose
